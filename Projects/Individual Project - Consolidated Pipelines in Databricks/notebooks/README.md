# Exported Databricks Notebooks

These files were exported from:

```text
/Shared/consolidated_pipeline
```

Databricks notebooks are stored as Python source files so they can be reviewed clearly in GitHub.

## Folders

| Folder | Contents |
| --- | --- |
| `setup_folder` | Catalog setup, schema utilities, and date dimension creation |
| `2_dimension_data_processing` | Customer, product, and pricing dimension pipelines |
| `3_fact_data_processing` | Full load and incremental load order fact pipelines |
